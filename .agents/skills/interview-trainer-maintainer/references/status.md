# Current status — 2026-08-31

Source files remain authoritative; keep this file short.

- Bank: 985 generated questions (725 Java backend, 260 AI/Agent), 422 registered sources; 366 are interview sources. Java backend has 145 concepts across 12 peer topics, each with five question angles.
- Study UX: detailed beginner answers, 8-stage/140-question backend path and 7-stage/101-question Agent path, recall queue, progress, mistakes, favorites, notes, themes, AI tutor.
- DeepSeek handoff added: update-center UI; `scripts/updater.mjs`; local Chrome login/cookie capture; fetch/extraction result cache; formula + AI score review; review/apply/backup/rollback flow; three Xiaohongshu-related interview sources.
- Current maintenance pass completed: real server-side cancellation, single-flight analysis, prompt-versioned SHA-256 cache, repeat-URL and dynamic-concept de-duplication, unique source/backup IDs, existing-source evidence patches, evidence-only apply, `.local/` initialization, two isolated regression tests, and this reusable maintainer Skill.
- Critical apply path verified and hardened: short AI fields are normalized, drafts are prevalidated, duplicate clicks are blocked, and isolated API/browser flows complete the current 795 → 800 transition with all five new questions searchable.
- Interrupted-update recovery completed: every finished source is cached immediately; users can finalize completed results without waiting for slow sources; validated review drafts are atomically persisted, restored after page/server restart, and cleared after apply/discard.
- Full-chain updater audit completed: previews are byte-read-only; auto/manual ingestion, duplicate-input collapse, compatible/balanced analysis, cancellation, single-flight, partial finalize, restart recovery, pending-draft protection, apply, evidence-only second run, secret redaction, cookie masking/clear, and byte-equivalent rollback are isolated regressions. UI state now resets partial-finalize controls, preserves a draft when discard fails, refreshes after external cancellation, and prevents a pending draft from being silently replaced; rollback is blocked during analysis.
- Local-state reliability audit completed: API keys expose `saved`/`session`/`none` without returning secrets; the UI warns and confirms before dropping restart persistence; config, cookies, cache, history, drafts, browser state, generated sidecars, and question/source files use atomic replacement. Complete draft temp files are promoted on restart, interrupted apply/rollback work is journaled and auto-restored from a validated backup, and browser progress now warns on storage failure and origin changes.
- Cross-browser login handoff completed in v0.1.1: the update page lists only server-detected browser IDs; Chrome, Edge, Brave, and Chromium use separate project-local profiles with automatic cookie collection, while Firefox and the system default browser open in explicit manual-Cookie mode. Executable paths cannot be supplied or exposed through the page, the preference is local to the browser origin, and static assets now revalidate after upgrades to prevent mixed old/new UI code.
- Source audit and targeted training completed in v0.2.0: `/api/sources` is field-whitelisted, `/api/insights` exposes sample coverage/company/role/concept trends, and the UI shows company and role lists, 90-day trend signals, platform/date/exclusion/attention limits, and full source cards. Xiaohongshu company interviews are correctly distinguished from Xiaohongshu platform coverage (currently zero).
- Updater source refreshes now persist collection method/time, trend eligibility, explicit engagement, candidate metadata, and quality warnings; missing model metadata no longer downgrades known source facts. Apply history records refreshed sources and rollback remains byte-equivalent.
- Explicit source discovery completed: user-found http/https links are canonicalized and persisted only in `.local/source-candidates.json`, dynamically marked pending/registered, selectable by opaque server-resolved IDs for the next analysis, and removable without changing registered sources. The queue does not claim to search a platform automatically.
- Beginner content phase 1 completed: all 985 questions now have angle-specific five-part explanations, actionable worked examples, and three question-specific follow-ups; 40 core concepts have curated code/command/SQL/pseudocode examples. Explicit review now covers all 144 current core questions (178 registered total, 807 outline), and regressions prevent shared five-angle answer bodies, missing examples/follow-ups, or unreviewed core IDs.
- High-frequency content phase 2 completed: Java concurrency, MySQL/transactions, Redis, system design, RAG, and Agent reliability cover 51 concepts and 201 high-tier questions. It added 40 curated examples and locked all six phase scopes, reviews, code examples, and source IDs; phase-4 totals supersede its intermediate counts.
- Learning-effectiveness phase 3 completed: progress v4 adds six bounded failure reasons and the latest 12 attempt records with ≤600-character answer previews; weak diagnosis excludes unseen/mastered questions, summarizes category/angle/reason signals, and creates a balanced 20-question focus queue. Version 2/3 imports remain compatible. Pure regressions and a temporary-port browser flow cover two low ratings, persisted history, mistake diagnostics, focus training, 390px no overflow, and an empty console.
- High-frequency content phase 4 completed: all 144 core and 423 high questions now have explicit angle-specific reviews; 43 new curated examples bring the enhancement map to all 123 core/high concepts. Eighteen official/research sources cover TCP/DNS/HTTP, Linux, Java GC, Spring, RabbitMQ, CAP, OpenTelemetry, Transformer, JSON Schema, multi-Agent, Agent Skills, and SSE without entering interview frequency. Current status is 567 reviewed / 418 extended outline; browser QA confirms a reviewed TCP detail, curated command/source rendering, 390px no overflow, and no console warnings/errors.
- Updater evaluation/telemetry phase completed: 10 manually labeled synthetic cases lock evidence grounding across positive, cross-concept, fabricated/short evidence, and partial-batch cases. Optional local telemetry is off by default and records only stage/result counts plus latency aggregates; server regression proves it contains no prompt/output marker, and browser QA confirms the unchecked setting with no console warnings/errors.
- Taxonomy and self-directed training shipped in v0.3.0: Java backend content is organized into 12 peer interview topics with a second-level `topicGroup`; all pre-v0.4 question IDs remain stable. Catalog filters combine topic, knowledge group, concept, five question angles, and auditable company-interview associations. Custom study sets save a fixed question snapshot plus per-topic concepts, angles, tier, count, and strategy; progress export is version 4 and still imports version 2/3.
- v0.4.0 release pass completed: night-theme Toast/select contrast is explicit, custom-study UI typography is larger, every topic can expand all current matches with answer excerpts and included/candidate badges, and saved-session sanitization no longer truncates at the old 795-question ceiling.
- Custom-study preview interaction was corrected after v0.4.1: title clicks alone open the library detail, while separate 12px `收入题单`/`已收录` controls apply sanitized per-question include/exclude overrides without leaving the custom view. Overrides survive save/reload/edit, library browsing text is larger, and the 390px layout has no horizontal overflow.
- Mainstream coverage audit completed against JavaGuide, Xiaolin Coding, Mianshiya's public recent hot list/Java 200 bank, and registered Nowcoder evidence. Thirty-eight backend concepts were appended without renumbering old IDs; `test-coverage.mjs` locks 123 checklist items across all 12 topics and prevents learning sources from entering interview frequency.
- Large-sample discovery and scoring completed: 10,302 public-Sitemap pages converge to 350 accepted interviews; 132 are frequency-eligible and 121 remain independent direct samples after series/repost/aggregate handling. `discuss` and `feed/main/detail` are interleaved, China-calendar dates come only from the main post, wrong-role titles are rejected, 30,000 discovery results are versioned/checkpointed, and the unchanged rerun is 10,302/10,302 cache hits with zero AI calls. Saturated scoring plus the separately capped public-title signal leaves the bank at 985 questions (144 core, 423 high, 418 extended); the 300-source AI stub regression remains at ≤38 first-run calls and zero repeat calls.
- Public-question attention re-screen completed: Mianshiya's public TOP50 metadata and 620 public titles are snapshotted without VIP answers; all 559 in-scope titles map to 143 existing concepts. Bank/title de-duplication, the +2 cap, low-confidence labeling, unknown-concept rejection, and zero interview-frequency leakage are regression-tested.
- AI extraction semantic protection completed: every model-derived concept now needs an exact source quote plus target-name/synonym grounding before cache or evidence linkage; unsupported mappings receive targeted single-source correction, all single-source structural/model failures retry once with compressed text, and three consecutively unusable batches open a circuit breaker. The update report exposes accepted/rejected evidence, rechecks, and circuit fallback. Isolated regressions reject a HashMap-to-B+tree mismatch, preserve unaffected batch items, cache corrected fallbacks, and keep the ideal 300-source path at 38/0 calls.
- Verified after phases 4–5: `npm run verify`, 985 questions, 422 sources, 567 explicit reviews, 123 enhanced core/high concepts, 10-case extraction evaluation, 12-topic/145-concept taxonomy, five angles, stable IDs, updater 985 → 990 → byte-equivalent rollback, 300-source performance at 38 first-run/0 repeat AI calls, and server API recovery paths.
- Release usability and automation completed for v0.4.1: README distinguishes formal assets from GitHub source archives; occupied starting ports fall forward up to 20 attempts; matching version tags run verification, verify the official Node.js runtime SHA-256, build both assets, generate checksums, and publish through GitHub Actions. The published lightweight ZIP has 41 allowlisted entries/413,864 bytes/SHA-256 `CC9404C01C815DE53BCB6A9B7B9E769A1C4AA81D8A4A5411DAB754B982B50EFB`; the published Windows x64 portable ZIP has 44 entries/35,809,934 bytes/SHA-256 `83A554B983F575E70945FB93625AC84460591AC789420A65C1715F7D34BB6CA9`. Both public downloads match `SHA256SUMS.txt`, contain no forbidden paths, and the downloaded portable package was proven to serve 985 questions/51 sources using its bundled Node.js 24.16.0 executable.
- v0.5.0 published and independently re-downloaded: the 49-entry lightweight ZIP is 644,023 bytes/SHA-256 `EAB9DCC94C33B7ABDD2F3D20D130738C233EB500CDAC48B262B09A149253905A`; the 52-entry Windows x64 ZIP is 36,040,093 bytes/SHA-256 `9052DE9347E910A1D85AFBFD4A96ABABAB705A6CAF2543F3D1F957A0C841956C`. Both match the published checksum file, contain no forbidden paths, and serve 985 questions/398 sources/559-of-559 public-title coverage after download; the portable package uses the verified official Node.js 24.16.0 runtime.
- v0.6.0 published and independently re-downloaded: the 55-entry lightweight ZIP is 1,049,426 bytes/SHA-256 `78B6E3A983C8B6EE7DD146CD368AAC58F28ABFBCCAD30CC7545CDAAD0B490663`; the 58-entry Windows x64 ZIP is 36,445,496 bytes/SHA-256 `5390E52404F4C0F7345FADCF1FE9CF066A8AEB72737CC38ADBAF73FFD2641080`. Both match the published checksum file, contain no forbidden paths, and the downloaded portable package serves 985 questions/422 sources using bundled Node.js 24.16.0.
- Next priorities: expand the labeled extraction set only from real anonymized failures; refine extended questions by measured demand; keep release verification reproducible.
- After every pass: record only verified deltas and the next 1–3 priorities here.
